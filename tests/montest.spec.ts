
import { test, expect } from '@playwright/test';

test('Ajouter un livre à une playlist via le modal à partir du composant Card', async ({ page }) => {
  await page.goto('http://localhost:5173/');


});
